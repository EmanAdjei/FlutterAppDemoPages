import 'package:flutter/material.dart';

class OptionButton {

  // Properties needed for each card
  String buttonDescription;
  Icon buttonIcon;
  String buttonName;

  //Constructor for OptionButton Class
  OptionButton({this.buttonName, this.buttonIcon, this.buttonDescription});

}